{
  "name": "a-workbench",
  "version": "0.26.1",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "marked": "^16.4.2",
    "turbo": "^2.10.12"
  },
  "dependencies": {
    "pg": "^8.21.0"
  },
  "engines": {
    "node": ">=22"
  }
}
