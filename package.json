{
  "name": "a-workbench",
  "version": "0.27.0",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "packages/*",
    "site"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:docs": "node --test docs/site/test/*.test.mjs",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "marked": "^16.4.2",
    "turbo": "^2.10.12"
  },
  "dependencies": {
    "pg": "^8.21.0"
  },
  "engines": {
    "node": ">=22"
  }
}
