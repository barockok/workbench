{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"] },
    "test": { "dependsOn": ["build"], "env": ["ENCRYPTION_KEY", "NODE_ENV"] },
    "lint": {},
    "dev": { "cache": false, "persistent": true }
  }
}
